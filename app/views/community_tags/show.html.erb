<p id="notice"><%= notice %></p>

<p>
  <strong>Tag:</strong>
  <%= @community_tag.tag_id %>
</p>

<p>
  <strong>Community:</strong>
  <%= @community_tag.community_id %>
</p>

<%= link_to 'Edit', edit_community_tag_path(@community_tag) %> |
<%= link_to 'Back', community_tags_path %>
