<%= form_with(model: community, local: true) do |form| %>
  <% if community.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(community.errors.count, "error") %> prohibited this community from being saved:</h2>

      <ul>
      <% community.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= form.label :コミュニティ名 %>
    <%= form.text_field :name, id: :community_name %>
    <%= form.fields_for :tags do |df| %>
    <%= df.label :タグ名 %>
    <%= df.text_field :name, id: :tag_name %>
   <% end %>
  </div>
  
  <div class="actions">
    <%= form.submit "作成"%>
  </div>
<% end %>
